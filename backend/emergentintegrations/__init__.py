# Local stub for the private `emergentintegrations` SDK (not available on public PyPI).
# This lets the backend import and boot. The LLM chat endpoints raise a clear
# runtime error when invoked until the real package is installed.
